/* Defines the route paths for the Master Router.
 * @typedef {Object} MasterRouterRouteMap
 */
export enum MasterRouterRouteMap {
  CONFIG_ROUTE = "/configs",
  COIN_ROUTE = "/coins",
}

export enum coinsRouteEndpoints {
  COINS_LIST = "/list",
}
